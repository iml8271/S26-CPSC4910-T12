Prerequisies:
Python 3.13.5

STEPS TO FOLLOW:
git clone <repo>
cd <repo>
python -m venv venv
source venv/bin/activate  (or venv\Scripts\activate on Windows)
pip install -r requirements.txt
flask run OR python3 app.python
open 'http://127.0.0.1:5000' in a new tab

